# merge_train Roadmap

## Recent activity (rolling)

- 2026-05-20 — **Not-Proven gap closure (subagent dispatch)**: 5 parallel subagents addressed all 7 Not Proven items with new tests + runner enhancements. Test count: 228 → 299 (all passing). New test files: `test_merge_queue_stress.py` (7 tests — 50-PR concurrent contention, reserve_plan atomicity, flock race safety, concurrent release integrity, 25-PR co-tenancy, whole-domain exclusion at scale, check under mutation), `test_long_running_reliability.py` (7 tests — 10-batch sequential cycles, lock leak detection, interleaved batch cleanup, stale lock detection, log integrity, bounded active_all), `test_mixed_workload.py` (29 tests — Python 55-function scale, Markdown scale, mixed Python+Markdown domains, multi-domain reserve_plan rollback, YAML/JSON fallback, cross-domain partial conflict, PR diff symbol enrichment), `test_semantic_adversarial.py` (28 tests — heading boundary off-by-one, malformed markdown, empty sections, adjacent function boundaries, decorator ranges, slug stability, heading-special-chars, adjacent-symbol no-false-collision, multi-symbol hunk spanning, re-reserve after release). Runner: `e2e_ao_orchestrated_runner.py` now supports `--keep-sessions` (NP6), `--parallel N` / `--parallel-batch-size` (NP7), `--session-wait-timeout`. Items NP1 (20-slot AO) is code-ready but requires a live run; NP6/NP7 require live AO infrastructure to produce evidence bundles.
- 2026-05-19 — **E2E proof with real OpenCode agents + AO scale proof**: 20 real `openw run` agents concurrently edited disjoint sections of the same Markdown file using auto-extracted area locks. Follow-up AO proof created 15 PRs through `ao spawn --agent claude-code` with isolated slot edits and session cleanup. Proof doc: [`docs/e2e_area_lock_proof.md`](../docs/e2e_area_lock_proof.md). Evidence bundles: [`evidence/v0.4/`](../evidence/v0.4/) and [`evidence/v0.5-ao/`](../evidence/v0.5-ao/). Remaining gap: 20-slot AO proof in production-style orchestration.
- 2026-05-18 — Fixed parser backward-compat: `domain_lock <cmd> --files X --git-cwd Y` (global flags after the subcommand) was rejected with `unrecognized arguments`. Fix in [`ce47114`](https://github.com/jleechanorg/merge_train/commit/ce47114) re-registers `--registry`, `--log`, `--git-cwd` on every subparser with `default=argparse.SUPPRESS`; follow-up [`15e2d0c`](https://github.com/jleechanorg/merge_train/commit/15e2d0c) added `reserve-plan` + `--registry` override coverage after adversarial code-review. Tests: 125 → 134. Evidence bundle: `/tmp/merge_train_evidence/proofs/parser_compat_fix/`. `/es` evidence-standards review and `/er` evidence-reviewer subagent both **PASS**.
- 2026-05-17 — Ran `/nextsteps` after a strict evidence review of `/tmp/merge_train_evidence/`. Current verdict: `merge_train` is a promising conflict-reduction primitive, but not yet proven as reliable pipeline infrastructure. Follow-up priorities are tracked as `orch-i7uv`, `orch-xxvz`, `orch-jf9s`, and `orch-66my`.
