__pycache__/
*.pyc
.pytest_cache/
.venv/
venv/
*.egg-info/
dist/
build/
.coverage
htmlcov/
.DS_Store
.idea/
.vscode/
*.swp
# Local lock log artifacts from running tests
/tmp_lock_test/
# Generated by install.sh — contain machine-specific absolute paths
.codex/
.gemini/
.opencode.json
# Claude Code machine-local state (worktrees, settings.local.json, memory cache)
.claude/
# Agent sealed-holdout test results (machine-local)
results/
# uv lockfile — package is a library, lockfile would lock transitive deps
uv.lock
# Session-local scratch research (sweep reports, throwaway analysis).
# Promote useful helpers to scripts/ instead.
scratch/
